version: '3.8'

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: portfolio_web
    restart: always
    ports:
      - "80:3000"
    environment:
      - DATABASE_URL=${DATABASE_URL}
